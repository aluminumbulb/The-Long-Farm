using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Genotype : MonoBehaviour
{
    public float seedingRadius, polinatingRadius, seedTime, pollinationTime, lifespan, whitherRate, minHeight, maxHeight;
    public int reproductivePotential;
    public Color[] plantColor;
}
